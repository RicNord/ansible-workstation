#- name: Install AUR packages
#  tags:
#    - arch
#    - packages
#  kewlfft.aur.aur:
#    name: "{{ item }}"
#    state: present
#  become: true
#  become_user: aur_builder
#  loop: "{{ lookup('file', 'files/aur.txt').splitlines() }}"

- name: Install AUR packages
  tags:
    - arch
    - packages
  kewlfft.aur.aur:
    name:
      - azure-cli
      - google-chrome
      - neovim-git
      - slack-desktop
    state: present
  become: true
  become_user: aur_builder
